apiVersion: v1
kind: Service
metadata:
  name: {{ include "job-scout.fullname" . }}
  labels:
    {{- include "job-scout.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
    - name: metrics
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "job-scout.selectorLabels" . | nindent 4 }}
